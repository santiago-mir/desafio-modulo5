{
  "name": "desafio-modulo5",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "gh-pages": "^4.0.0",
    "parcel": "^2.8.2"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir build --public-url /desafio-modulo5",
    "deploy":"gh-pages -d ./build",
    "build-and-deploy": "yarn build && yarn deploy"
  }
}
